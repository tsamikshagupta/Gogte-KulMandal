MONGODB_URI="mongodb+srv://<user>:<pass>@<cluster-host>/?retryWrites=true&w=majority"
MONGODB_DB="FamilyMembers"

# Core collections
MONGODB_COLLECTION="BalkrishnaNivasSample"   # family members collection
MONGODB_LOGIN_COLLECTION="login"             # login/users accounts
MONGODB_TEMP_MEMBERS_COLLECTION="tempmembers"# pending members approval

# Optional: if you store users in a different collection name
# MONGODB_USERS_COLLECTION="users"

# Auth
JWT_SECRET="change_this_to_a_long_random_value"

# Admin/DBA fallback credentials (used by /api/auth/login)
DBA_EMAIL="dba@gmail.com"
DBA_PASSWORD="dba123"
ADMIN_EMAIL="admin@gmail.com"
ADMIN_PASSWORD="admin123"

# SMTP (for emailing credentials)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
FROM_EMAIL="no-reply@gogte-kulamandal.local"

# Optional: secondary notification email for dual-project emails
# SECONDARY_PROJECT_EMAIL=""

# Server
PORT="4000"

